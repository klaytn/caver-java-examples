description 'boiilerPlate for Contract layer'

task deploy_smart_contract(type: JavaExec) {
    group = "BoilerPlate for caver.contract"
    classpath = sourceSets.main.runtimeClasspath
    main = "deploy_smart_contract.BoilerPlate"
}

task execute_smart_contract(type: JavaExec) {
    group = "BoilerPlate for caver.contract"
    classpath = sourceSets.main.runtimeClasspath
    main = "execute_smart_contract.BoilerPlate"
}

task fee_delegation(type: JavaExec) {
    group = "BoilerPlate for caver.contract"
    classpath = sourceSets.main.runtimeClasspath
    main = "fee_delegation.BoilerPlate"
}

